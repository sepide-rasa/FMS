@using Ext.Net.MVC;

@(
 Html.X().Window().Height(600).Width(917)
        .Title("پیش نمایش")
        .AutoScroll(true)
        .Closable(true)
        .CloseAction(Ext.Net.CloseAction.Destroy)
                .Html("<center><object style='width: 900px;height: 566px;border: 1px solid #ccc;' type='application/pdf' data='/Daramad/Mokatebat/GeneratePDF?FileId=" + ViewBag.FileId + "'></object></center>")
)
