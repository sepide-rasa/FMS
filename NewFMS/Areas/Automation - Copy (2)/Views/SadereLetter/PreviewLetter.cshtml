@using Ext.Net.MVC;

@(
 Html.X().Window()
        .ID("PreviewLetter")
    .Modal(true)
    .Width(900)
    .Height(600)
    .Title("پیش نمایش نامه")
    .AutoScroll(true)
    .Closable(true)
   /// .Maximized(true)
        .CloseAction(Ext.Net.CloseAction.Destroy)//.Listeners(l => l.AfterRender.Handler = "LoadPreviewLetter();")
      .Html("<center><object class='se-pre-con' style='width: 100%;height:565px;border: 1px solid #ccc;' type='application/pdf' data='/Automation/SadereLetter/GeneratePDFPreviewLetter?LetterId=" + ViewBag.LetterId +  "'></object></center>")
)
<script>
    function LoadPreviewLetter() {
        var a = $('object');
        var h = App.SpecificKartabl.height;
        a.height(h-35);
    }
</script>

